spring:
  application:
    name: autocoder
  datasource:
    url: jdbc:postgresql://localhost:5432/MyDB?currentSchema=autocoder
    username: postgres
    password: Swapnil@2001
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    schemas: autocoder